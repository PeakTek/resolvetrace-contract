{
  "name": "@peaktek/resolvetrace-contract",
  "version": "0.1.0",
  "description": "ResolveTrace public contract: TypeBox schema sources, emitted JSON Schemas, and OpenAPI spec. Authority repository; not published to npm.",
  "license": "Apache-2.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build:schemas": "tsx scripts/build-schemas.ts",
    "validate:fixtures": "tsx scripts/validate-fixtures.ts",
    "validate:openapi": "tsx scripts/validate-openapi.ts",
    "validate:sdk-output": "tsx scripts/validate-sdk-output.ts",
    "selftest:dumper": "tsx scripts/dumper-self-test.ts",
    "typecheck": "tsc --noEmit",
    "check:all": "npm run typecheck && npm run build:schemas && npm run validate:fixtures && npm run validate:openapi && npm run selftest:dumper && npm run validate:sdk-output"
  },
  "dependencies": {
    "ajv": "^8.17.1",
    "ajv-formats": "^3.0.1"
  },
  "devDependencies": {
    "@apidevtools/json-schema-ref-parser": "^15.4.0",
    "@seriousme/openapi-schema-validator": "^2.4.0",
    "@sinclair/typebox": "^0.34.49",
    "@types/node": "^26.0.1",
    "prettier": "^3.9.4",
    "tsx": "^4.23.0",
    "typescript": "^6.0.3"
  }
}
